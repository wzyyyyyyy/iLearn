<ui:FluentWindow
    x:Class="iLearn.Views.Windows.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:iLearn.ViewModels.Windows"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:iLearn.Views.Windows"
    mc:Ignorable="d"
    Width="420"
    Height="600"
    WindowStartupLocation="CenterScreen"
    WindowCornerPreference="Round"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    ResizeMode="NoResize"
    Loaded="Window_Loaded"
    d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel, IsDesignTimeCreatable=True}"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="KeyDown">
            <i:InvokeCommandAction Command="{Binding KeyPressCommand}" PassEventArgsToCommand="True"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <local:StepToVisibilityConverter x:Key="StepToVisibility"/>

        <Storyboard x:Key="CardLoadStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="50" To="0" Duration="0:0:1">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <ui:TitleBar VerticalAlignment="Top" Margin="0,0,0,0" />

        <Rectangle Fill="#0078D4" Opacity="0.05" RadiusX="20" RadiusY="20" Margin="0,0,0,280"/>

        <Grid Margin="0,32,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 页眉 -->
            <StackPanel Grid.Row="0" VerticalAlignment="Center" x:Name="HeaderPanel" Opacity="0">
                <StackPanel.RenderTransform>
                    <TranslateTransform Y="0"/>
                </StackPanel.RenderTransform>

                <ui:ImageIcon Source="pack://application:,,,/Assets/iLearn.png"
                              Width="120" Height="120" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                <TextBlock x:Name="HeaderSubtitle"
                           Text="请登录您的账户"
                           FontSize="13"
                           Opacity="0.8"
                           Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,16"/>
            </StackPanel>

            <ContentPresenter x:Name="RootContentDialogPresenter" Grid.Row="0" />

            <!-- ═══ Step 1: 账号密码 ═══ -->
            <ui:Card Grid.Row="1" Margin="32,0,32,20"
                     Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                     BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
                     BorderThickness="1" x:Name="LoginCard" Opacity="0"
                     Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=Password}">
                <ui:Card.RenderTransform>
                    <TranslateTransform Y="0"/>
                </ui:Card.RenderTransform>

                <StackPanel Margin="28,28,28,24">
                    <ui:TextBox x:Name="UsernameTextBox"
                           PlaceholderText="请输入用户名"
                           Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                           Margin="0,0,0,16"
                           Cursor="IBeam"
                           Validation.ErrorTemplate="{x:Null}">
                        <ui:TextBox.Icon>
                            <ui:SymbolIcon Symbol="Person24" Filled="True" />
                        </ui:TextBox.Icon>
                    </ui:TextBox>

                    <ui:PasswordBox x:Name="PasswordBox"
                        PlaceholderText="请输入密码"
                        Password="{Binding UserPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,0,0,6"
                        Validation.ErrorTemplate="{x:Null}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="KeyDown">
                                <i:InvokeCommandAction Command="{Binding KeyPressCommand}" PassEventArgsToCommand="True"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <ui:PasswordBox.Icon>
                            <ui:SymbolIcon Symbol="Password24" Filled="True" />
                        </ui:PasswordBox.Icon>
                    </ui:PasswordBox>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" Content="记住我" IsChecked="{Binding IsRememberMeEnabled}" Command="{Binding ClickCheckBoxCommand}">
                            <CheckBox.ToolTip>
                                <ToolTip Content="下次登录时自动填充用户名"/>
                            </CheckBox.ToolTip>
                        </CheckBox>

                        <CheckBox Grid.Column="1" Content="自动登录" IsChecked="{Binding IsAutoLoginEnabled}" Margin="15,0,0,0" Command="{Binding ClickCheckBoxCommand}">
                            <CheckBox.ToolTip>
                                <ToolTip Content="下次启动应用时自动登录"/>
                            </CheckBox.ToolTip>
                        </CheckBox>
                    </Grid>

                    <ui:Button x:Name="LoginButton"
                               Content="登 录"
                               Appearance="Primary"
                               Command="{Binding LoginCommand}"
                               Height="42" HorizontalAlignment="Stretch"
                               FontSize="14" FontWeight="Medium" Margin="0,8,0,0">
                        <ui:Button.Resources>
                            <ResourceDictionary>
                                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#0069BF"/>
                                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#005CA6"/>
                            </ResourceDictionary>
                        </ui:Button.Resources>
                        <ui:Button.Style>
                            <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect ShadowDepth="1" BlurRadius="8" Opacity="0.3" Color="#0078D4"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Setter Property="Background" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Style>
                        </ui:Button.Style>
                    </ui:Button>

                    <Border Margin="0,20,0,0" Height="1" Background="{ui:ThemeResource ControlStrokeColorDefaultBrush}" Opacity="0.5"/>

                    <TextBlock HorizontalAlignment="Center" Margin="0,12,0,0">
                        <Hyperlink NavigateUri="https://ilearntec.jlu.edu.cn/" RequestNavigate="Hyperlink_RequestNavigate"
                                  Foreground="{ui:ThemeResource AccentTextFillColorPrimaryBrush}">
                            跳转官网
                            <Hyperlink.ToolTip>
                                <ToolTip Content="跳转学在吉大"/>
                            </Hyperlink.ToolTip>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </ui:Card>

            <!-- ═══ Step 2: 微信二次认证 ═══ -->
            <ui:Card Grid.Row="1" Margin="32,0,32,20"
                     Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                     BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
                     BorderThickness="1"
                     Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=WechatVerify}">

                <StackPanel Margin="28,20,28,20">

                    <!-- 提示信息 -->
                    <Border Background="#0D0078D4" CornerRadius="6" Padding="12,10" Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Info16" Margin="0,0,8,0" Foreground="#0078D4" VerticalAlignment="Center"/>
                            <TextBlock FontSize="12" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" MaxWidth="280">
                                输入图形验证码后点击"发送"，微信验证码将通过
                                <Run FontWeight="SemiBold" Foreground="#0078D4">智慧吉大</Run>
                                小程序推送
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- 图形验证码 -->
                    <TextBlock Text="图形验证码" FontSize="12"
                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ui:TextBox Grid.Column="0"
                                    PlaceholderText="请输入图形验证码"
                                    Text="{Binding ImageCaptchaCode, UpdateSourceTrigger=PropertyChanged}"
                                    Margin="0,0,8,0">
                            <ui:TextBox.Icon>
                                <ui:SymbolIcon Symbol="ShieldKeyhole20"/>
                            </ui:TextBox.Icon>
                        </ui:TextBox>

                        <Border Grid.Column="1" Width="100" Height="38" CornerRadius="4"
                                BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1" Cursor="Hand"
                                ToolTip="点击刷新图形验证码">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" Command="{Binding RefreshCasCaptchaCommand}"/>
                            </Border.InputBindings>
                            <Image Source="{Binding CaptchaImage}" Stretch="Uniform"/>
                        </Border>
                    </Grid>

                    <!-- 微信验证码 -->
                    <TextBlock Text="微信验证码" FontSize="12"
                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ui:TextBox Grid.Column="0"
                                    PlaceholderText="请输入微信验证码"
                                    Text="{Binding WechatCode, UpdateSourceTrigger=PropertyChanged}"
                                    Margin="0,0,8,0">
                            <ui:TextBox.Icon>
                                <ui:SymbolIcon Symbol="Chat16"/>
                            </ui:TextBox.Icon>
                        </ui:TextBox>

                        <ui:Button Grid.Column="1"
                                   Content="{Binding SendWechatButtonText}"
                                   Command="{Binding SendWechatCodeCommand}"
                                   IsEnabled="{Binding CanSendWechatCode}"
                                   Width="100" Height="38"
                                   FontSize="12"/>
                    </Grid>

                    <!-- 验证按钮 -->
                    <ui:Button Content="验 证 登 录"
                               Appearance="Primary"
                               Command="{Binding VerifyWechatCommand}"
                               Height="42" HorizontalAlignment="Stretch"
                               FontSize="14" FontWeight="Medium" Margin="0,0,0,12">
                        <ui:Button.Resources>
                            <ResourceDictionary>
                                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#0069BF"/>
                                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#005CA6"/>
                            </ResourceDictionary>
                        </ui:Button.Resources>
                        <ui:Button.Style>
                            <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                <Setter Property="Background" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Style>
                        </ui:Button.Style>
                    </ui:Button>

                    <!-- 返回按钮 -->
                    <Button Content="← 返回重新输入密码"
                            Command="{Binding BackToPasswordCommand}"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="{ui:ThemeResource AccentTextFillColorPrimaryBrush}"
                            FontSize="12"
                            HorizontalAlignment="Center"
                            Cursor="Hand"/>
                </StackPanel>
            </ui:Card>

            <!-- 加载遮罩 -->
            <Grid x:Name="ProgressOverlay"
                  Grid.Row="0" Grid.RowSpan="2"
                  Visibility="{Binding IsAuthenticationInProgress, Converter={StaticResource BoolToVisibility}}"
                  Panel.ZIndex="1000">
                <Grid.RenderTransformOrigin>0.5, 0.5</Grid.RenderTransformOrigin>
                <Grid.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Grid.RenderTransform>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Opacity" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="Visibility" Value="Visible">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                From="0" To="1" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                From="1" To="0" Duration="0:0:0.25"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Rectangle Fill="{ui:ThemeResource ApplicationBackgroundBrush}" Opacity="0.8">
                    <Rectangle.Effect>
                        <BlurEffect KernelType="Gaussian" Radius="20" RenderingBias="Quality"/>
                    </Rectangle.Effect>
                </Rectangle>

                <Border Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Width="180" Height="120"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="15" Opacity="0.2"/>
                    </Border.Effect>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ui:ProgressRing Grid.Row="0"
                                         IsIndeterminate="True"
                                         Width="40" Height="40"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="1"
                                   Text="正在登录中..."
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,16"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
    </Grid>
</ui:FluentWindow>
